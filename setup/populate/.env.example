# ── Neo4j connection (required) ──────────────────────────────────────────────
NEO4J_URI=neo4j+s://xxxxxxxx.databases.neo4j.io
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your-password-here

# ── AWS Bedrock Embeddings (used during load command) ───────────────────────
# Model used to generate embeddings for Requirement and Defect descriptions.
EMBEDDING_MODEL_ID=amazon.titan-embed-text-v2:0
REGION=us-west-2

# ── Samples (samples command) ───────────────────────────────────────────────
# Number of rows returned per section when running sample queries
# SAMPLE_SIZE=10
